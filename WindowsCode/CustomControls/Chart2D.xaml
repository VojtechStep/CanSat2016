<UserControl
    x:Class="WindowsCode.CustomControls.Chart2D"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsCode.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Name="MainCanvas" Background="{ThemeResource SystemAccentColor}" Width="{x:Bind Width}" Height="{x:Bind Height}">
        <TextBlock Name="GraphLabel" Text="{x:Bind Header}" HorizontalAlignment="Center" Padding="0" Margin="5" />
        <Grid Name="PlotArea" Margin="{x:Bind PlotAreaMargin}" Background="{x:Bind PlotAreaBackground}" Width="{x:Bind PlotAreaWidth}" Height="{x:Bind PlotAreaHeight}" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
